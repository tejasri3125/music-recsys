# No API keys needed — iTunes preview lookup and the mood parser are
# both free/local with zero configuration required.
#
# If you deploy this, update the CORS allow_origins list in app/main.py
# to include your real frontend domain (e.g. your Cloudflare Pages URL)
# instead of just localhost.
